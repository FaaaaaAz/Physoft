# ============================================
# Environment Variables - Physoft Backend
# ============================================

# Server port
PORT=3000

# Database URL (PostgreSQL — required, see prisma/schema.prisma)
DATABASE_URL="postgresql://user:password@localhost:5432/physoft?schema=public"

# Runtime environment
NODE_ENV=development

# Allowed CORS Origins (comma separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Cloudinary Configuration (for image uploads)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# AI Provider Configuration
# Valores permitidos: gemini | openai
AI_PROVIDER=gemini

# Gemini (required if AI_PROVIDER=gemini)
GEMINI_API_KEY=your_gemini_api_key
GEMINI_MODEL=gemini-2.5-flash

# OpenAI (required if AI_PROVIDER=openai)
OPENAI_API_KEY=your_openai_api_key
OPENAI_MODEL=gpt-4.1-mini
OPENAI_TIMEOUT_MS=60000

# Anthropic Claude (required for /api/claude/analyze-assessment)
ANTHROPIC_API_KEY=your_anthropic_api_key
ANTHROPIC_MODEL=claude-sonnet-5

# JWT Secret (required — generate a long random string, e.g. `openssl rand -base64 48`)
JWT_SECRET=your-super-secure-secret-key-here
JWT_EXPIRES_IN=7d

# Initial admin user, auto-created on first backend startup if it doesn't
# already exist yet (never overwrites an existing user's password on
# subsequent restarts — only Change Password in the app can update it).
ADMIN_EMAIL=admin@physoft.com
ADMIN_PASSWORD=change-me-before-first-deploy
